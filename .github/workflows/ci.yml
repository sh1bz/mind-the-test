# CI only. The live site (untilitsticks.com) builds on Cloudflare Pages from main; nothing deploys from here.
name: CI
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: npm }
      - run: npm ci
      - run: npm test
      - run: npm run check
        env:
          PUBLIC_PAY_LINK: ${{ vars.PUBLIC_PAY_LINK }}
          PUBLIC_GADS_ID: ${{ vars.PUBLIC_GADS_ID }}
          PUBLIC_GADS_UNLOCK: ${{ vars.PUBLIC_GADS_UNLOCK }}
          PUBLIC_GADS_START: ${{ vars.PUBLIC_GADS_START }}
          PUBLIC_GADS_TEN: ${{ vars.PUBLIC_GADS_TEN }}
          PUBLIC_GADS_DATE: ${{ vars.PUBLIC_GADS_DATE }}
      - run: npm run build
        env:
          PUBLIC_SUPABASE_URL: ${{ vars.PUBLIC_SUPABASE_URL }}
          PUBLIC_SUPABASE_ANON_KEY: ${{ vars.PUBLIC_SUPABASE_ANON_KEY }}
          PUBLIC_PAY_LINK: ${{ vars.PUBLIC_PAY_LINK }}
          PUBLIC_GADS_ID: ${{ vars.PUBLIC_GADS_ID }}
          PUBLIC_GADS_UNLOCK: ${{ vars.PUBLIC_GADS_UNLOCK }}
          PUBLIC_GADS_START: ${{ vars.PUBLIC_GADS_START }}
          PUBLIC_GADS_TEN: ${{ vars.PUBLIC_GADS_TEN }}
          PUBLIC_GADS_DATE: ${{ vars.PUBLIC_GADS_DATE }}
